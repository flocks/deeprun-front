import colors from './colors';

const global = {
    label: {
        fontSize: '13px',
        fontStyle: 'italic',
        marginTop: '5px',
        color: colors.label
    }
};

export default global;
